# Custom Buildpack Test

This buildpack is a fork of the open source java buildpack. It combines the python buildpack to create a hybrid java/python environment, then adds parts of the QNX sdk (GDB).
